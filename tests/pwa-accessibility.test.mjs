import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';
test('service worker excludes APIs and authenticated navigations',async()=>{const sw=await readFile(new URL('../public/sw.js',import.meta.url),'utf8');assert.match(sw,/startsWith\('\/api\/'\)/);assert.match(sw,/request\.mode==='navigate'/);assert.match(sw,/SKIP_WAITING/);assert.match(sw,/conte-os-feitos-v4/);assert.doesNotMatch(sw,/SHELL=\['\/'/)});
test('layout has keyboard skip and live connection status',async()=>{const[layout,status,css]=await Promise.all(['app/layout.tsx','app/PwaStatus.tsx','app/quality.css'].map(x=>readFile(new URL(`../${x}`,import.meta.url),'utf8')));assert.match(layout,/Pular para o conteúdo/);assert.match(layout,/id="main-content"/);assert.match(status,/aria-live="polite"/);assert.match(css,/prefers-reduced-motion/);assert.match(css,/:focus-visible/)});
