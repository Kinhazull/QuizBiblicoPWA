import{requireUser,type AppEnv}from"../../_lib/auth";import{json}from"../../_lib/security";
export const onRequestGet=async({request,env}:{request:Request;env:AppEnv})=>{try{const user:any=await requireUser(request,env),rows=await env.DB.prepare(`SELECT action,created_at createdAt FROM audit_logs WHERE actor_user_id=?1 AND action IN ('user.password_changed','account.self_recovered','recovery_codes.generated','privacy.deletion_requested') ORDER BY created_at DESC LIMIT 30`).bind(user.id).all();return json({events:rows.results})}catch(response){if(response instanceof Response)return response;throw response;}};
