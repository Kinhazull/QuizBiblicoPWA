import{requireUser,type AppEnv}from"../../_lib/auth";import{json}from"../../_lib/security";
export const onRequestGet=async({request,env,params}:{request:Request;env:AppEnv;params:{id:string}})=>{try{const viewer:any=await requireUser(request,env);const target:any=await env.DB.prepare(`SELECT id,display_name AS displayName,nickname,use_nickname_in_ranking AS useNicknameInRanking,profile_public AS profilePublic,role,created_at AS createdAt FROM users WHERE id=?1 AND organization_id=?2 AND status='active'`).bind(params.id,viewer.organizationId).first();if(!target)return json({error:'not_found'},404);if(!target.profilePublic&&target.id!==viewer.id&&!['admin','leader'].includes(viewer.role))return json({error:'private_profile'},403);const stats:any=await env.DB.prepare(`SELECT COUNT(DISTINCT round_id) roundsPlayed,COALESCE(MAX(score),0) bestScore,COALESCE(MAX(max_streak),0) bestStreak,COALESCE(SUM(correct_answers),0) totalCorrect FROM attempts WHERE user_id=?1 AND mode='official' AND status='completed'`).bind(target.id).first();return json({profile:{id:target.id,displayName:target.useNicknameInRanking&&target.nickname?target.nickname:target.displayName,createdAt:target.createdAt},stats});}catch(response){if(response instanceof Response)return response;throw response;}};
