import{requireAdmin,type AppEnv}from"../../../_lib/auth";import{json,verifyPassword}from"../../../_lib/security";
export const onRequestGet=async({request,env,params}:{request:Request;env:AppEnv;params:{id:string}})=>{try{const admin:any=await requireAdmin(request,env);const round:any=await env.DB.prepare("SELECT * FROM rounds WHERE id=?1 AND organization_id=?2").bind(params.id,admin.organizationId).first();if(!round)return json({error:'not_found'},404);const found=await env.DB.prepare("SELECT * FROM questions WHERE round_id=?1 ORDER BY position").bind(params.id).all();const questions=[];for(const q of found.results as any[]){const choices=await env.DB.prepare("SELECT id,text,correct FROM choices WHERE question_id=?1").bind(q.id).all();questions.push({...q,choices:choices.results})}const attempts:any=await env.DB.prepare("SELECT COUNT(*) total,COUNT(DISTINCT user_id) participants FROM attempts WHERE round_id=?1 AND status='completed'").bind(params.id).first();return json({round,questions,attempts});}catch(response){if(response instanceof Response)return response;throw response;}};
export const onRequestPatch=async({request,env,params}:{request:Request;env:AppEnv;params:{id:string}})=>{try{const admin:any=await requireAdmin(request,env),body:any=await request.json();const current:any=await env.DB.prepare("SELECT r.*,(SELECT COUNT(*) FROM attempts a WHERE a.round_id=r.id) attempt_count FROM rounds r WHERE r.id=?1 AND r.organization_id=?2").bind(params.id,admin.organizationId).first();if(!current)return json({error:'not_found'},404);const status=String(body.status||current.status);if(!['draft','scheduled','active','closed','cancelled'].includes(status))return json({error:'invalid_status'},400);const now=Date.now(),opensAt=status==='active'&&body.releaseNow?now:body.opensAt?new Date(body.opensAt).getTime():current.opens_at,closesAt=body.closesAt?new Date(body.closesAt).getTime():current.closes_at;if(!Number.isFinite(opensAt)||!Number.isFinite(closesAt)||closesAt<=opensAt)return json({error:'invalid_schedule'},400);const editingDetails=body.title!==undefined||body.theme!==undefined||body.description!==undefined||body.secondsPerQuestion!==undefined||body.opensAt!==undefined||body.closesAt!==undefined;if(editingDetails&&(Number(current.attempt_count)>0||!['draft','scheduled'].includes(current.status)))return json({error:'round_locked'},409);const title=String(body.title??current.title).trim(),theme=String(body.theme??current.theme).trim(),description=body.description===undefined?current.description:String(body.description).trim(),seconds=Math.max(15,Math.min(60,Number(body.secondsPerQuestion??current.seconds_per_question)));if(title.length<3||theme.length<3)return json({error:'invalid_fields'},400);await env.DB.prepare("UPDATE rounds SET title=?1,theme=?2,description=?3,status=?4,opens_at=?5,closes_at=?6,seconds_per_question=?7,updated_at=?8 WHERE id=?9 AND organization_id=?10").bind(title,theme,description||null,status,opensAt,closesAt,seconds,now,params.id,admin.organizationId).run();await env.DB.prepare("INSERT INTO audit_logs (id,organization_id,actor_user_id,action,entity_type,entity_id,details_json,created_at) VALUES (?1,?2,?3,'round.updated','round',?4,?5,?6)").bind(crypto.randomUUID(),admin.organizationId,admin.id,params.id,JSON.stringify({status,editingDetails}),now).run();return json({ok:true});}catch(response){if(response instanceof Response)return response;throw response;}};
export const onRequestDelete=async({request,env,params}:{request:Request;env:AppEnv;params:{id:string}})=>{try{const admin:any=await requireAdmin(request,env),body:any=await request.json();const c:any=await env.DB.prepare("SELECT password_hash,password_salt FROM users WHERE id=?1").bind(admin.id).first();if(!c||!await verifyPassword(String(body.password||''),c.password_salt,c.password_hash))return json({error:'invalid_password'},403);const count:any=await env.DB.prepare("SELECT COUNT(*) total FROM attempts WHERE round_id=?1").bind(params.id).first();if(Number(count?.total||0)>0){await env.DB.prepare("UPDATE rounds SET status='cancelled',updated_at=?1 WHERE id=?2 AND organization_id=?3").bind(Date.now(),params.id,admin.organizationId).run();return json({ok:true,archived:true})}await env.DB.prepare("DELETE FROM rounds WHERE id=?1 AND organization_id=?2").bind(params.id,admin.organizationId).run();return json({ok:true,deleted:true});}catch(response){if(response instanceof Response)return response;throw response;}};
