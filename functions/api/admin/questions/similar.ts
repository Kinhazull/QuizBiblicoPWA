import type{AppEnv}from"../../../_lib/auth";import{requireAnyPermission}from"../../../_lib/permissions";import{json}from"../../../_lib/security";
const words=(s:string)=>new Set(s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9 ]/g,' ').split(/\s+/).filter(w=>w.length>2));const score=(a:Set<string>,b:Set<string>)=>{const both=[...a].filter(x=>b.has(x)).length,all=new Set([...a,...b]).size;return all?both/all:0};
export const onRequestGet=async({request,env}:{request:Request;env:AppEnv})=>{try{const user:any=await requireAnyPermission(request,env,['questions.edit','questions.review']),rows=await env.DB.prepare("SELECT id,prompt,reference,theme FROM question_bank WHERE organization_id=?1 AND status<>'archived' ORDER BY updated_at DESC LIMIT 600").bind(user.organizationId).all(),items=rows.results as any[],pairs:any[]=[];for(let i=0;i<items.length;i++){const a=words(items[i].prompt);for(let j=i+1;j<items.length;j++){const b=words(items[j].prompt),similarity=score(a,b);if(similarity>=.72)pairs.push({first:items[i],second:items[j],similarity:Math.round(similarity*100)})}}pairs.sort((a,b)=>b.similarity-a.similarity);return json({pairs:pairs.slice(0,100),scanned:items.length})}catch(response){if(response instanceof Response)return response;throw response;}};
