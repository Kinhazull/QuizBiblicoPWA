import type { AppEnv } from "../../../_lib/auth";
import { requirePermission } from "../../../_lib/permissions";
import { getAdminEvent, updatePlatformEvent } from "../../../_lib/platform-events";
import { json } from "../../../_lib/security";
import { PublicErrorCategory, publicDomainError, publicError } from "../../../_lib/operational-observability";
type Context = { request: Request; env: AppEnv; params: { id: string } };
export const onRequestGet = async ({ request, env, params }: Context) => { try { const user: any = await requirePermission(request, env, "rounds.manage"); const event = await getAdminEvent(env, user.organizationId, params.id); return event ? json({ event }, 200, { "cache-control": "no-store, private" }) : json({ error: "event_not_found" }, 404); } catch (error) { return publicError(error, { category: PublicErrorCategory.INTERNAL_ERROR, code: "event_read_failed", component: "admin-events", operation: "read_event" }); } };
export const onRequestPatch = async ({ request, env, params }: Context) => { try { const user: any = await requirePermission(request, env, "rounds.manage"); const event = await updatePlatformEvent(env, { organizationId: user.organizationId, userId: user.id }, params.id, await request.json()); return json({ event }, 200, { "cache-control": "no-store, private" }); } catch (error) { return publicDomainError(error, { event_not_found: { category: PublicErrorCategory.NOT_FOUND, status: 404 }, event_locked: { category: PublicErrorCategory.CONFLICT, status: 409 }, invalid_event: { category: PublicErrorCategory.VALIDATION_ERROR, status: 400 }, invalid_event_games: { category: PublicErrorCategory.VALIDATION_ERROR, status: 400 }, invalid_event_content: { category: PublicErrorCategory.VALIDATION_ERROR, status: 400 }, invalid_event_content_count: { category: PublicErrorCategory.VALIDATION_ERROR, status: 400 } }, { component: "admin-events", operation: "update_event" }); } };
