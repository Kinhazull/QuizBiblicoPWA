"use client";import{useEffect}from"react";export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error(error)},[error]);return <main className="shell error-screen" role="alert"><section><span>⚠️</span><p className="eyebrow">ALGO NÃO SAIU COMO ESPERADO</p><h1>Vamos tentar <em>novamente</em></h1><p>Se o problema continuar, verifique sua conexão e retorne à tela inicial.</p><button className="primary" onClick={reset}>TENTAR NOVAMENTE</button><a href="/">VOLTAR AO INÍCIO</a>{error.digest&&<small>Código: {error.digest}</small>}</section></main>}
