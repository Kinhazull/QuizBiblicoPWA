"use client";
import { useState } from "react";
export default function Setup(){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch('/api/setup/bootstrap',{method:'POST',headers:{'content-type':'application/json','x-bootstrap-secret':String(data.secret)},body:JSON.stringify(data)});const result=await response.json();setMessage(response.ok?'Configuração concluída. Você já pode entrar como administrador.':result.error==='already_initialized'?'Esta plataforma já foi configurada.':'Confira os dados e a chave de inicialização.');setBusy(false)}return <main className="shell auth-screen"><section className="auth-card setup-card"><header className="brand"><span className="brand-dot">✦</span> CONTE OS FEITOS</header><p className="eyebrow">PRIMEIRO ACESSO</p><h1>Configure sua <em>comunidade</em></h1><p className="intro">Esta tela funciona apenas uma vez e cria o administrador, o grupo e o primeiro convite.</p><form onSubmit={submit}><label>Nome da organização<input name="organizationName" required minLength={3}/></label><label>Nome do grupo<input name="groupName" defaultValue="Jovens" required/></label><label>Seu nome<input name="displayName" required/></label><label>Usuário administrador<input name="username" required/></label><label>Senha do administrador<input name="password" type="password" minLength={10} required/></label><label>Primeiro código de convite<input name="inviteCode" minLength={6} required/></label><label>Chave de inicialização<input name="secret" type="password" required/></label>{message&&<p className="auth-message">{message}</p>}<button className="primary" disabled={busy}>{busy?'CONFIGURANDO...':'CRIAR PLATAFORMA'}<span>→</span></button></form><a className="auth-switch" href="/">Voltar ao login</a></section></main>}
