"use client";
import { useState } from "react";
export default function Setup(){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const data=Object.fromEntries(new FormData(e.currentTarget));const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),15000);try{const response=await fetch('/api/setup/bootstrap',{method:'POST',headers:{'content-type':'application/json','x-bootstrap-secret':String(data.secret)},body:JSON.stringify(data),signal:controller.signal});const text=await response.text();let result:any={};try{result=JSON.parse(text)}catch{}if(response.ok){setMessage('Configuração concluída! Redirecionando para o login...');setTimeout(()=>location.href='/',1800)}else if(result.error==='already_initialized')setMessage('Esta plataforma já foi configurada.');else if(result.error==='forbidden')setMessage('A chave de inicialização está incorreta.');else setMessage(`Não foi possível configurar${result.error?`: ${result.error}`:'. Tente novamente.'}`)}catch(error){setMessage(error instanceof DOMException&&error.name==='AbortError'?'A configuração excedeu 15 segundos. Tente novamente.':'Falha de comunicação com o servidor.')}finally{clearTimeout(timer);setBusy(false)}}return <main className="shell auth-screen"><section className="auth-card setup-card"><header className="brand"><span className="brand-dot">✦</span> CONTE OS FEITOS</header><p className="eyebrow">PRIMEIRO ACESSO</p><h1>Configure sua <em>comunidade</em></h1><p className="intro">Esta tela funciona apenas uma vez e cria o administrador, o grupo e o primeiro convite.</p><form onSubmit={submit}><label>Nome da organização<input name="organizationName" required minLength={3}/></label><label>Nome do grupo<input name="groupName" defaultValue="Jovens" required/></label><label>Seu nome<input name="displayName" required/></label><label>Usuário administrador<input name="username" required/></label><label>Senha do administrador<input name="password" type="password" minLength={10} required/></label><label>Primeiro código de convite<input name="inviteCode" minLength={6} required/></label><label>Chave de inicialização<input name="secret" type="password" required/></label>{message&&<p className="auth-message" role="status">{message}</p>}<button className="primary" disabled={busy}>{busy?'CONFIGURANDO...':'CRIAR PLATAFORMA'}<span>→</span></button></form><a className="auth-switch" href="/">Voltar ao login</a></section></main>}
