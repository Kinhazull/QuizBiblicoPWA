"use client";
import { useEffect, useState } from "react";
export default function Reports(){const[rounds,setRounds]=useState<any[]>([]),[roundId,setRoundId]=useState("");useEffect(()=>{fetch("/api/admin/rounds").then(r=>{if(r.status===401||r.status===403){location.href="/";return null}return r.json()}).then(data=>data&&setRounds(data.rounds||[]))},[]);const download=(type:string)=>{location.href=`/api/admin/exports?type=${type}${type==="results"&&roundId?`&roundId=${encodeURIComponent(roundId)}`:""}`};return <main className="admin-shell"><section className="admin-title"><p className="eyebrow">RELATÓRIOS</p><h1>Exportar <em>dados</em></h1><p>Baixe arquivos CSV compatíveis com Excel, mantendo os dados sob controle da administração.</p></section><section className="report-grid"><article className="admin-panel"><b>👥</b><h2>Membros</h2><p>Nome, usuário, apelido, situação e datas de acesso.</p><button onClick={()=>download("members")}>BAIXAR MEMBROS</button></article><article className="admin-panel"><b>🏆</b><h2>Ranking geral</h2><p>Pontuação acumulada, média e rodadas concluídas.</p><button onClick={()=>download("ranking")}>BAIXAR RANKING</button></article><article className="admin-panel"><b>📝</b><h2>Resultados</h2><p>Tentativas, pontos, acertos, tempo e sequência.</p><select value={roundId} onChange={e=>setRoundId(e.target.value)}><option value="">Todas as rodadas</option>{rounds.map(round=><option key={round.id} value={round.id}>{round.title}</option>)}</select><button onClick={()=>download("results")}>BAIXAR RESULTADOS</button></article><article className="admin-panel"><b>🛡️</b><h2>Histórico administrativo</h2><p>Últimas cinco mil ações registradas para auditoria.</p><button onClick={()=>download("audit")}>BAIXAR HISTÓRICO</button></article></section></main>}
