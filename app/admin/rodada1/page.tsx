"use client";import{useState}from"react";export default function RoundOne(){const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function publish(){setBusy(true);const r=await fetch('/api/admin/round-one',{method:'POST'});const d=await r.json();setMsg(r.ok?`Rodada 1 publicada. Encerra em ${new Date(d.closesAt).toLocaleString('pt-BR')}.`:d.error==='already_exists'?'A Rodada 1 já foi publicada.':'Não foi possível publicar.');setBusy(false)}return <main className="admin-shell"><header className="admin-head"><div className="brand"><span className="brand-dot">✦</span> CONTE OS FEITOS</div><a href="/admin/rodadas/lista">Rodadas</a></header><section className="admin-title"><p className="eyebrow">LANÇAMENTO INICIAL</p><h1>Publicar a <em>Rodada 1</em></h1><p>As dez perguntas da V1 serão liberadas imediatamente, com 20 segundos por pergunta e encerramento no próximo domingo às 09:30.</p></section><section className="admin-panel"><h2>Contem o que Deus fez</h2><p>Testemunhos, milagres e os feitos de Deus.</p><button className="primary" onClick={publish} disabled={busy}>{busy?'PUBLICANDO...':'PUBLICAR RODADA 1'}</button>{msg&&<p className="auth-message">{msg}</p>}</section></main>}
