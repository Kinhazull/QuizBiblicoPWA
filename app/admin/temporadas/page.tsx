"use client";import{useEffect,useState}from"react";const q=(n:number)=>`${n}º trimestre`;export default function AdminSeasons(){const[items,setItems]=useState<any[]>([]),[year,setYear]=useState(new Date().getFullYear()),[quarter,setQuarter]=useState(Math.floor(new Date().getMonth()/3)+1),[title,setTitle]=useState(''),[message,setMessage]=useState('');async function load(){const r=await fetch('/api/admin/seasons');if(!r.ok){location.href='/admin';return}setItems((await r.json()).seasons||[])}useEffect(()=>{load()},[]);async function create(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/seasons',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({year,quarter,title:title||`${q(quarter)} de ${year}`})}),d=await r.json();setMessage(r.ok?'Temporada criada.':d.error==='season_exists'?'Já existe uma temporada neste trimestre.':'Confira os dados.');if(r.ok){setTitle('');load()}}return <main className="admin-shell"><section className="admin-title"><p className="eyebrow">CICLOS TRIMESTRAIS</p><h1>Gerenciar <em>temporadas</em></h1><p>Planeje, acompanhe e congele a classificação final de cada trimestre.</p></section>{message&&<p className="auth-message">{message}</p>}<form className="admin-panel season-form" onSubmit={create}><label>Ano<input type="number" min="2020" max="2100" value={year} onChange={e=>setYear(Number(e.target.value))}/></label><label>Trimestre<select value={quarter} onChange={e=>setQuarter(Number(e.target.value))}>{[1,2,3,4].map(v=><option value={v} key={v}>{q(v)}</option>)}</select></label><label>Nome opcional<input value={title} onChange={e=>setTitle(e.target.value)} placeholder={`${q(quarter)} de ${year}`}/></label><button className="primary">CRIAR TEMPORADA</button></form><section className="admin-panel season-list">{items.map(item=><article key={item.id}><div><small>{item.year} · {q(item.quarter)}</small><h2>{item.title}</h2><p>{new Date(item.starts_at).toLocaleDateString('pt-BR',{timeZone:'America/Sao_Paulo'})} até {new Date(item.ends_at).toLocaleDateString('pt-BR',{timeZone:'America/Sao_Paulo'})} · {item.roundCount} rodada(s)</p></div><b>{item.status}</b><footer><a href={`/admin/temporadas/detalhes?id=${item.id}`}>PAINEL E DETALHES</a></footer></article>)}</section></main>}
