"use client";
import{useEffect,useState}from"react";
export default function Notifications(){const[data,setData]=useState<any>(null);async function load(){const r=await fetch('/api/notifications');if(r.status===401){location.href='/';return}setData(await r.json())}useEffect(()=>{load()},[]);async function read(key:string,href?:string){await fetch('/api/notifications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({key})});if(href)location.href=href;else load()}async function readAll(){await fetch('/api/notifications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({all:true})});load()}if(!data)return <main className="admin-shell"><p>Carregando avisos...</p></main>;return <main className="admin-shell notification-page"><section className="admin-title"><p className="eyebrow">AVISOS</p><h1>Suas <em>notificações</em></h1><p>{data.unread?`${data.unread} aviso(s) ainda não lido(s).`:'Você está em dia com os avisos.'}</p></section>{data.unread>0&&<button className="read-all" onClick={readAll}>MARCAR TODAS COMO LIDAS</button>}<section className="notification-list">{data.notifications.map((item:any)=><article key={item.key} className={`admin-panel ${item.read?'read':'unread'}`}><b>{item.icon}</b><div><small>{item.read?'LIDO':'NOVO'}</small><h2>{item.title}</h2><p>{item.message}</p></div><button onClick={()=>read(item.key,item.href)}>ABRIR</button></article>)}{!data.notifications.length&&<div className="admin-panel empty-notifications"><b>✓</b><h2>Nenhum aviso por enquanto</h2><p>Novas rodadas, prazos e conquistas aparecerão aqui.</p></div>}</section></main>}
