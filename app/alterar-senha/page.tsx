"use client";
import { useState } from "react";
export default function ChangePassword(){
 const [message,setMessage]=useState(""),[busy,setBusy]=useState(false);
 async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const data=Object.fromEntries(new FormData(event.currentTarget));if(data.newPassword!==data.confirmPassword){setMessage("As novas senhas não coincidem.");setBusy(false);return}const response=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const result=await response.json();if(response.ok){setMessage("Senha alterada. Redirecionando...");setTimeout(()=>location.href="/",1000)}else setMessage(result.error==="invalid_current_password"?"A senha temporária está incorreta.":"Use pelo menos 8 caracteres e escolha uma senha diferente.");setBusy(false)}
 return <main className="shell auth-screen"><section className="auth-card"><header className="brand"><span className="brand-dot">✦</span> CONTE OS FEITOS</header><p className="eyebrow">SEGURANÇA</p><h1>Crie sua nova <em>senha</em></h1><p className="intro">Por segurança, substitua a senha temporária antes de continuar.</p><form onSubmit={submit}><label>Senha temporária<input name="currentPassword" type="password" required autoFocus/></label><label>Nova senha<input name="newPassword" type="password" minLength={8} required/></label><label>Confirme a nova senha<input name="confirmPassword" type="password" minLength={8} required/></label>{message&&<p className="auth-message">{message}</p>}<button className="primary" disabled={busy}>{busy?"SALVANDO...":"ALTERAR SENHA"}<span>→</span></button></form></section></main>;
}
