"use client";import{useState}from"react";
export default function Recover(){const[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const d=Object.fromEntries(new FormData(e.currentTarget));if(d.password!==d.confirm){setMsg('As senhas não coincidem.');setBusy(false);return}const r=await fetch('/api/auth/recover',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:d.username,code:d.code,password:d.password})});if(r.ok)setMsg('Senha redefinida. Você já pode voltar e entrar.');else setMsg('Código inválido, já utilizado ou dados incorretos.');setBusy(false)}return <main className="shell auth-screen"><section className="auth-card"><header className="brand"><span className="brand-dot">✦</span> CONTE OS FEITOS</header><p className="eyebrow">RECUPERAÇÃO SEGURA</p><h1>Volte para sua <em>jornada</em></h1><p className="intro">Use um dos códigos pessoais que você guardou no perfil.</p><form onSubmit={submit}><label>Nome de usuário<input name="username" required autoCapitalize="none"/></label><label>Código de recuperação<input name="code" required placeholder="XXXXX-XXXXX" autoCapitalize="characters"/></label><label>Nova senha<input name="password" type="password" required minLength={8}/></label><label>Repita a nova senha<input name="confirm" type="password" required minLength={8}/></label>{msg&&<p className="auth-message">{msg}</p>}<button className="primary" disabled={busy}>{busy?'AGUARDE...':'REDEFINIR SENHA'}</button></form><a className="auth-switch" href="/">Voltar ao login</a></section></main>}
