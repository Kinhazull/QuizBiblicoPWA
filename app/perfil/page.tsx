"use client";import{useEffect,useState}from"react";export default function Profile(){const[data,setData]=useState<any>(null),[message,setMessage]=useState("");useEffect(()=>{fetch('/api/profile/me').then(async r=>{if(r.status===401){location.href='/';return}setData(await r.json())})},[]);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/profile/me',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({nickname:d.nickname,useNicknameInRanking:d.useNicknameInRanking==='on',profilePublic:d.profilePublic==='on'})});setMessage(r.ok?'Perfil atualizado com sucesso.':'Não foi possível atualizar.')}if(!data)return <main className="admin-shell"><p>Carregando perfil...</p></main>;const{user,stats}=data;return <main className="admin-shell profile-page"><section className="admin-title"><p className="eyebrow">MEU PERFIL</p><h1>{user.nickname||user.displayName}</h1><p>{stats.roundsPlayed||0} rodada(s) concluída(s)</p></section><section className="profile-stats"><article><small>MELHOR PONTUAÇÃO</small><strong>{Number(stats.bestScore||0).toLocaleString('pt-BR')}</strong></article><article><small>ACERTOS</small><strong>{stats.totalCorrect||0}</strong></article><article><small>MAIOR SEQUÊNCIA</small><strong>{stats.bestStreak||0}</strong></article><article><small>PÓDIOS</small><strong>{stats.podiums||0}</strong></article></section><form className="admin-panel profile-form" onSubmit={save}><h2>Como quero aparecer</h2><label>Apelido<input name="nickname" maxLength={30} defaultValue={user.nickname||''} placeholder="Ex.: Kinhazul"/></label><label className="check-row"><input name="useNicknameInRanking" type="checkbox" defaultChecked={Boolean(user.useNicknameInRanking)}/> Usar meu apelido nos rankings</label><label className="check-row"><input name="profilePublic" type="checkbox" defaultChecked={Boolean(user.profilePublic)}/> Permitir que participantes vejam meu perfil</label><p className="privacy-note">Seu nome de usuário e dados de acesso nunca aparecem para outros participantes.</p><button className="primary">SALVAR PERFIL</button>{message&&<p className="auth-message">{message}</p>}</form><a className="profile-medals" href="/medalhas">🎖️ Ver minhas 50 medalhas</a></main>}
