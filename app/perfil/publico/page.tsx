"use client";import{useEffect,useState}from"react";export default function PublicProfile(){const[data,setData]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{const id=new URL(location.href).searchParams.get('id');if(!id){setError('Perfil não encontrado.');return}fetch(`/api/profile/${id}`).then(async r=>{if(r.status===401){location.href='/';return}const d=await r.json();if(!r.ok)setError(d.error==='private_profile'?'Este perfil é privado.':'Perfil não encontrado.');else setData(d)})},[]);if(error)return <main className="admin-shell"><section className="admin-title"><h1>Perfil <em>indisponível</em></h1><p>{error}</p></section></main>;if(!data)return <main className="admin-shell"><p>Carregando...</p></main>;const{profile,stats}=data;return <main className="admin-shell"><section className="admin-title"><p className="eyebrow">PERFIL DA JORNADA</p><h1>{profile.displayName}</h1><p>Participa desde {new Date(profile.createdAt).toLocaleDateString('pt-BR')}</p></section><section className="profile-stats"><article><small>RODADAS</small><strong>{stats.roundsPlayed||0}</strong></article><article><small>MELHOR PONTUAÇÃO</small><strong>{Number(stats.bestScore||0).toLocaleString('pt-BR')}</strong></article><article><small>ACERTOS</small><strong>{stats.totalCorrect||0}</strong></article><article><small>MAIOR SEQUÊNCIA</small><strong>{stats.bestStreak||0}</strong></article></section></main>}
